spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:tc:postgresql:11.5.0://localhost/dev_test_vehicle

  flyway:
    schemas: public
    locations: filesystem:doc/flyway/migrations,classpath:/sql